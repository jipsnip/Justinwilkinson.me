import React, {Component} from 'react';
import styles from '../../Stylesheets/League/LeagueTextInput.css';

export default class LeagueRecommendation extends Component{
    constructor(props){
        super(props);

        this.state = {
            hovered: false,
            favorited: false
        };
    }

    render(){
        return (
            <div>
            </div>
        );
    }
}